
# WARN: # https://starship.rs/config/
# vimcmd_symbol is only supported in cmd, fish and zsh.
# vimcmd_replace_one_symbol, vimcmd_replace_symbol, and vimcmd_visual_symbol are
# only supported in fish due to upstream issues with mode detection in zsh.
